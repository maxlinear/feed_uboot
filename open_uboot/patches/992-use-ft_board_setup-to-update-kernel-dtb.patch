Index: u-boot-2022.01/board/maxlinear/lightning/pre_kern_fixups.c
===================================================================
--- u-boot-2022.01.orig/board/maxlinear/lightning/pre_kern_fixups.c
+++ u-boot-2022.01/board/maxlinear/lightning/pre_kern_fixups.c
@@ -9,6 +9,9 @@
 #include <fdt_support.h>
 #include <asm/io.h>
 #include <asm/arch/nrst.h>
+#include <asm/global_data.h>
+
+DECLARE_GLOBAL_DATA_PTR;
 
 #define BOOTARGS_EXTRA_LEN 30
 
@@ -80,9 +83,8 @@ static ulong get_fdt_loadaddr(void)
 }
 
 /* Function to read CMA size from device tree and update the bootargs */
-int read_cma_size(void)
+int read_cma_size(const void *fdt)
 {
-	const void *fdt;
 	int node_offset, len, bootargs_len;
 	const char *prop;
 	char *str_bootargs = NULL;
@@ -91,16 +93,12 @@ int read_cma_size(void)
 	char *str_rootfs_alignment = NULL;
 	u32 rootfs_load_addr = 0;
 	ulong cma_size, cma_alignment, cma_start, rootfs_alignment;
-	ulong loadaddr = 0;
 
-	/* Get the FDT load address using the common API */
-	loadaddr = get_fdt_loadaddr();
-	if (!loadaddr) {
-		printf("[%s] Error: Failed to get FDT load address\n", __func__);
+	if (!fdt) {
+		printf("[%s] Error: Invalid FDT address\n", __func__);
 		return -1;
 	}
 
-	fdt = (void *)loadaddr;
 	node_offset = fdt_path_offset(fdt, "/reserved-memory/linux,cma");
 	if (node_offset < 0) {
 		printf("[%s] Error: Cannot find /reserved-memory/linux,cma node, error code: %d\n",
@@ -289,18 +287,11 @@ int update_kernel_dtb_boot_bank(void)
  */
 int parse_os_cmdline(void)
 {
-#ifdef CONFIG_PRPL_BOOT_SEQUENCE
-	int ret;
-
-	/* Update kernel DTB with boot bank information */
-	ret = update_kernel_dtb_boot_bank();
-	if (ret < 0) {
-		printf("[%s] Warning: Failed to update kernel DTB with boot bank info\n",
-			__func__);
-		/* Don't return error, continue with CMA update */
-	}
-#endif /* CONFIG_PRPL_BOOT_SEQUENCE */
+	return 0;
+}
 
+int ft_board_setup(void *blob, struct bd_info *bd)
+{
 	/* Read CMA size from device tree and update the bootargs */
-	return read_cma_size();
+	return read_cma_size(blob);
 }
